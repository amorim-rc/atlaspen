# Dependências
/node_modules

# Artefatos de build do Astro
/dist
/.astro

# Saída da verificação do motor de atributos (npm run verificar)
/.verificar-build

# Configuração local de ferramentas
/.claude

# Conferidor: páginas baixadas do Planalto e relatórios de cada rodada.
# São reproduzíveis (scripts/robos/nucleo/baixar.py) e pesam ~5 MB por rodada; na
# CI viram artifact. As DECISOES-*.md do diretório continuam versionadas.
/crawler/snapshots
/crawler/relatorios
/crawler/proposta

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
__pycache__/

# O diretorio estudos/ saiu do repositorio em 24/09/2026, com a auditoria: o
# que tinha funcao mudou de lugar (o modelo dos atributos foi para docs/, e as
# licoes do jurisbot viraram o debito tecnico do README). O que sobrava era
# material de trabalho de uma revisao ja aplicada.
#
# As linhas abaixo ficam porque o diretorio continua sendo usado LOCALMENTE: os
# cinco documentos de pesquisa cedidos pela Dra. Luisa sao ineditos, e nao
# entram no versionamento enquanto ela nao decidir a forma de publicacao. Nem o
# codigo de terceiro trazido para estudo.
/estudos/
